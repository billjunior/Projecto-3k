# Arquivo de exemplo de variáveis de ambiente
# Copie este arquivo para .env.production e preencha com seus valores reais
# NUNCA commite .env.production no Git!

# Database
DATABASE_URL=postgresql://usuario:senha@host:5432/crm3k_production

# Rails
RAILS_ENV=production
RAILS_SERVE_STATIC_FILES=true
RAILS_LOG_TO_STDOUT=true
SECRET_KEY_BASE=gerar_com_rails_secret

# Email Configuration
# Para testes em desenvolvimento/staging, use Mailtrap (https://mailtrap.io)
# Para produção, use um serviço SMTP real (Gmail, SendGrid, Mailgun, etc.)

# Mailtrap (Desenvolvimento/Staging - Email Trap para testes)
SMTP_ADDRESS=sandbox.smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USERNAME=seu_username_mailtrap
SMTP_PASSWORD=sua_password_mailtrap

# Gmail (Produção - exemplo)
# SMTP_ADDRESS=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=seuemail@gmail.com
# SMTP_PASSWORD=sua_senha_de_app

# Mailer Settings
MAILER_HOST=localhost
MAILER_PORT=3000
MAILER_FROM=noreply@crm3k.com

# Application
APP_HOST=crm3k.seudominio.com
APP_PROTOCOL=https

# Storage (para Active Storage)
# Se usar Amazon S3:
AWS_ACCESS_KEY_ID=sua_access_key
AWS_SECRET_ACCESS_KEY=sua_secret_key
AWS_REGION=us-east-1
AWS_BUCKET=crm3k-production

# Ou para armazenamento local:
STORAGE_SERVICE=local
