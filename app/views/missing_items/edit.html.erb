<div class="container-fluid">
  <div class="row mb-4">
    <div class="col">
      <h1><i class="bi bi-pencil"></i> Editar Item em Falta</h1>
      <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
          <li class="breadcrumb-item"><%= link_to 'Dashboard', dashboard_path %></li>
          <li class="breadcrumb-item"><%= link_to 'Itens em Falta', missing_items_path %></li>
          <li class="breadcrumb-item active" aria-current="page">Editar</li>
        </ol>
      </nav>
    </div>
  </div>

  <div class="row">
    <div class="col-lg-8">
      <div class="card">
        <div class="card-header">
          <h5 class="mb-0">Editar: <%= @missing_item.item_name %></h5>
        </div>
        <div class="card-body">
          <%= render 'form', missing_item: @missing_item %>
        </div>
      </div>
    </div>

    <div class="col-lg-4">
      <div class="card border-warning">
        <div class="card-header bg-warning text-dark">
          <h5 class="mb-0"><i class="bi bi-exclamation-triangle"></i> Atenção</h5>
        </div>
        <div class="card-body">
          <p>Alterações neste item serão atualizadas imediatamente.</p>
          <p class="mb-0">Se o item foi resolvido, utilize o botão "Marcar como Resolvido" na visualização do item.</p>
        </div>
      </div>

      <div class="card border-info mt-3">
        <div class="card-header bg-info text-white">
          <h5 class="mb-0"><i class="bi bi-info-circle"></i> Informações</h5>
        </div>
        <div class="card-body">
          <p class="mb-2"><strong>Fonte:</strong> <%= @missing_item.source_text %></p>
          <p class="mb-2"><strong>Criado em:</strong><br><%= l(@missing_item.created_at, format: :long) %></p>
          <% if @missing_item.last_notified_at.present? %>
            <p class="mb-0"><strong>Última Notificação:</strong><br><%= l(@missing_item.last_notified_at, format: :long) %></p>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
