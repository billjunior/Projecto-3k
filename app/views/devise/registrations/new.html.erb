<% company_settings = Tenant.first&.company_setting %>

<style>
  .signup-wrapper {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    position: relative;
    overflow: hidden;
  }

  <% if company_settings&.logo&.attached? %>
  .signup-wrapper::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 80%;
    height: 80%;
    background-image: url('<%= url_for(company_settings.logo) %>');
    background-repeat: no-repeat;
    background-position: center;
    background-size: contain;
    opacity: 0.05;
    z-index: 0;
  }
  <% end %>

  .signup-card {
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
    border-radius: 20px;
    padding: 50px 40px;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
    width: 100%;
    max-width: 450px;
    position: relative;
    z-index: 1;
  }

  .signup-logo {
    text-align: center;
    margin-bottom: 30px;
  }

  .signup-logo img {
    max-width: 180px;
    max-height: 80px;
    margin-bottom: 15px;
  }

  .signup-logo h2 {
    color: #333;
    font-weight: 700;
    margin: 0;
    font-size: 28px;
  }

  .signup-logo p {
    color: #666;
    font-size: 14px;
    margin-top: 5px;
  }

  .signup-form .form-label {
    color: #444;
    font-weight: 600;
    margin-bottom: 8px;
    font-size: 14px;
  }

  .signup-form .form-control {
    border: 3px solid #FFC107;
    border-radius: 10px;
    padding: 12px 18px;
    font-size: 15px;
    transition: all 0.3s ease;
    background-color: #FFFEF5;
  }

  .signup-form .form-control:focus {
    border-color: #FFA000;
    box-shadow: 0 0 0 0.2rem rgba(255, 193, 7, 0.25);
    background-color: white;
  }

  .signup-form .btn-signup {
    width: 100%;
    padding: 14px;
    border-radius: 10px;
    font-weight: 600;
    font-size: 16px;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    border: none;
    color: white;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .signup-form .btn-signup:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 20px rgba(102, 126, 234, 0.4);
  }

  .signup-links {
    text-align: center;
    margin-top: 25px;
    padding-top: 25px;
    border-top: 1px solid #e0e0e0;
  }

  .signup-links a {
    color: #667eea;
    text-decoration: none;
    font-size: 14px;
    transition: color 0.3s ease;
  }

  .signup-links a:hover {
    color: #764ba2;
    text-decoration: underline;
  }

  .password-hint {
    font-size: 13px;
    color: #888;
    margin-top: 5px;
  }
</style>

<div class="signup-wrapper">
  <div class="signup-card">
    <div class="signup-logo">
      <% if company_settings&.logo&.attached? %>
        <%= image_tag company_settings.logo, alt: company_settings&.company_name || 'CRM 3K' %>
      <% end %>
      <h2>Criar Conta</h2>
      <p>Registre-se no sistema</p>
    </div>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: 'signup-form' }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="mb-3">
        <%= f.label :name, 'Nome Completo', class: 'form-label' %>
        <%= f.text_field :name, autofocus: true, autocomplete: "name", class: 'form-control', placeholder: 'Seu nome completo' %>
      </div>

      <div class="mb-3">
        <%= f.label :email, 'Email', class: 'form-label' %>
        <%= f.email_field :email, autocomplete: "email", class: 'form-control', placeholder: 'seu@email.com' %>
      </div>

      <div class="mb-3">
        <%= f.label :password, 'Senha', class: 'form-label' %>
        <%= f.password_field :password, autocomplete: "new-password", class: 'form-control', placeholder: '••••••••', id: 'password-input' %>
        <div class="password-hint">
          Mínimo 12 caracteres, incluindo: maiúscula, minúscula, número e caractere especial
        </div>
      </div>

      <div class="mb-3">
        <%= f.label :password_confirmation, 'Confirmar Senha', class: 'form-label' %>
        <%= f.password_field :password_confirmation, autocomplete: "new-password", class: 'form-control', placeholder: '••••••••' %>
      </div>

      <div class="mt-4">
        <%= f.submit "Criar Conta", class: 'btn btn-signup' %>
      </div>
    <% end %>

    <div class="signup-links">
      <%= link_to "← Voltar ao login", new_session_path(resource_name) %>
    </div>
  </div>
</div>
